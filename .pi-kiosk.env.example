# Copy to .pi-kiosk.env and replace these values for your Pi.
PI_HOST="${PI_HOST:-pi@raspberrypi.local}"
PI_SSH_KEY="${PI_SSH_KEY:-/path/to/private/key}"
PI_APP_DIR="${PI_APP_DIR:-/home/pi/pi-kiosk-vuejs}"
PI_USER_HOME="${PI_USER_HOME:-/home/pi}"
